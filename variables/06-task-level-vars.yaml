- name: task level vars 
  hosts: local
  connection: local
  vars:
    person: jack
    profession: "Product Designer"
  tasks:
  - name: play-level
    ansible.builtin.debug:
      msg: "PERSON IS: {{ person }}, PROFESSION IS: {{ profession }}"
  - name: task-level
    vars:
      person: lee 
      profession: "Business Analyst"
    ansible.builtin.debug:
      msg: "PERSON IS: {{ person }}, PROFESSION IS: {{ profession }}"